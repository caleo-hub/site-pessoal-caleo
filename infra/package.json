{
  "name": "site-pessoal-caleo-infra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit",
    "synth": "cdk synth --quiet",
    "ci": "npm run build && npm run synth",
    "deploy": "cdk deploy --require-approval never"
  },
  "dependencies": {
    "aws-cdk-lib": "2.261.0",
    "constructs": "10.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "aws-cdk": "2.1130.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
